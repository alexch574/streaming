<h1>You have a registered stream</h1>
<p>Stream key: <%= stream_key %> </p>
<p>Stream address: http://localhost:3000/streams/<%= stream_id %></p>
<p>Stream name: <%= name %></p>
<p>Update stream:</p>
<form action="/streams/create" method="POST">
    <div>
        <label for="name">Name</label>
        <input
          type="name"
          name="name"
          placeholder="name"
          value="<%= typeof name != 'undefined' ? name : '' %>"
        />
      
    </div>
    
      <button type="submit" class="btn btn-primary btn-block">
        Create
      </button>
    </form>
    
